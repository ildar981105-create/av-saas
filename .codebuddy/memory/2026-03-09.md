# 2026-03-09

## 视频译制页 Landing 功能 Tab 改造

### 改动内容
1. **translate-v4.html Landing 页**：
   - 将原来上传区下方的 3 个单功能卡片（擦除/字幕/配音）改为上传区**上方**的 4 个并列 Tab（一键译制/擦除字幕/添加字幕/克隆配音）
   - Tab 有选中态（底部渐变亮线 + 背景高亮 + 文字变亮）
   - 切换 Tab 时，标题、副标题、上传区提示文案跟随动态变化
   - 支持 `?mode=erase|subtitle|voice` URL 参数，页面加载自动选中对应 Tab
   - CSS: 新增 `.tv2-mode-tabs` / `.tv2-mode-tab` 样式，替代旧的 `.tv2-single-skills` 系列

2. **index.html 工作台**：
   - 第一排：三个一级功能卡片并列（一键译制、直播剪辑、AI 生成）
   - 第二排：新增"视频译制 · 单项功能"标注 + 三个子功能小卡片（擦除字幕、添加字幕、克隆配音）
   - 子功能使用 `.skill-card--sm` 样式，通过 `?mode=xxx` 链接到译制页
   - 放弃了 `.skill-card-group` 组合结构方案（用户反馈太奇怪）

3. **style.css**：
   - 新增 `.skill-card--sm`, `.skills-grid--sub`, `.skill-icon--red` 样式

## UI UX Pro Max Skill 全局 UI 重塑

### 设计系统
- **风格**: Glassmorphism + Dark Mode (OLED) + Motion-Driven
- **字体**: Space Grotesk (headings) + DM Sans (body) — "Tech Startup" 配对
- **主色**: Indigo (#6366f1) 替代纯紫色作为交互主色
- **辅色**: Purple (#a855f7) 用于渐变装饰, Emerald (#10b981) 用于 CTA/成功态
- **表面**: OLED 深色底 (#08080d → #0f0f1a → #141422 → #16162a)

### 改动范围 (style.css)
1. **全局设计令牌** — `:root` 新增 glass tokens, typography scale, spacing, transitions
2. **Sidebar** — glassmorphic 背景 + indigo active 指示器
3. **Skill Cards** — `::before` hover overlay, indigo 发光边框
4. **Home Input** — backdrop-filter blur, indigo 聚焦态
5. **Agent Entry** — indigo orb glow
6. **Smart Input** — glassmorphic 容器 + indigo focus
7. **Scenario Cards** — indigo hover + conic glow 动画
8. **Showcase Cards** — indigo tags + hover shadows
9. **Quick Actions** — indigo 高亮, Space Grotesk 标题
10. **Recent Tasks** — indigo 进度指示器
11. **Upload Zone** — indigo 边框 + hover
12. **Immersive Flow** — indigo hero glow + upload zone + buttons
13. **全局批量替换**: rgba(168,85,247) → rgba(99,102,241), #22c55e → #10b981, 硬编码颜色→CSS变量, 过渡/圆角→CSS令牌
14. **所有 HTML 文件** — Google Fonts 链接更新为 Space Grotesk + DM Sans

### HTML 内联样式批量升级（第二轮）
- 使用 Python 脚本批量处理 9 个 HTML 文件的内联 `<style>` 中的旧颜色值
- 总计 656 处颜色替换：
  - generate.html: 74 处
  - livestream.html: 11 处
  - translate-v4.html: 188 处
  - translate-v3.html: 185 处
  - translate-v2.html: 161 处
  - translate.html: 2 处
  - result.html: 33 处
  - editor.html: 1 处
  - editor-v2.html: 1 处
- 替换规则: rgba(168,85,247)→rgba(99,102,241), #a78bfa→#818cf8, #22c55e→#10b981, #4ade80→#34d399, rgba(34,197,94)→rgba(16,185,129), 硬编码文字颜色→CSS变量

### 部署
- 已推送到 GitHub Pages: https://ildar981105-create.github.io/av-saas/

### 备份
- 原文件备份在 `backup/app_backup_20260309_210350/`
